console.log(`Termux hızlı başlangıç:
1) pkg update && pkg upgrade
2) pkg install nodejs git cloudflared
3) cd ~ && git clone <repo-url> zaragram
4) cd zaragram
5) npm install
6) cp .env.example .env.local
7) npm run dev
8) Public erişim için npm run dev:public`);
